mixin point(opt)
	- opt.checked = opt.checked ? 'checked' : '';
	- opt.value = opt.value ? opt.value.toString() : opt.name.toString();
	li.point
		span.point__change
			input(id=opt.name name=opt.name type='checkbox' checked=opt.checked)
		label.point__label(for=opt.name)= opt.value
